__pycache__/
*.pyc
.venv/
venv/

# Secretos: cualquier variante de .env (la plantilla .env.example si se permite)
.env
.env.*
!.env.example

# Logs del servidor
*.log

# Estado en vivo del HUD (lo escribe jarvis_voz.py al correr)
hud/estado.json

# Datos personales y generados de la voz (NUNCA al historial):
# - memoria_charlas.md: las conversaciones de Luis con Jarvis
# - ultima_grabacion.wav: audio real del microfono (prueba_oido.py)
# - cache_voz/: mp3 sintetizados por el TTS
voz/memoria_charlas.md
voz/ultima_grabacion.wav
voz/cache_voz/

# Modelos descargados (40+ MB; el instalador los baja, no van al historial)
voz/modelos/
